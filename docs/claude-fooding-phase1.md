# Claude-fooding Phase 1 closeout (M31a–M31f)

Status checklist:

- [x] `--root` propagation fixed in manifest frontend invocation.
- [x] `workspace()` helper IR emission fixed.
- [x] `app` targets support explicit `types: ""`.
- [x] `tspack init` onboarding added.
- [x] `manifest-frontend` production build hygiene fixed.
- [x] `tspack check --json` added.
- [x] `tspack how` diagnostics remediation help added.
- [x] `tspack/manifest` authoring type surface and init-time local delivery added.

Phase 1 closeout intent: remove known authoring and onboarding blockers without changing runtime execution semantics.
