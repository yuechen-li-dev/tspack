# {{projectName}}

This project was created with `tspack init --template static`.

## Next steps

```sh
tspack update
tspack sync
tspack run dev
tspack check
```

The manifest is the project contract. `package.json` is a compatibility projection for familiar tooling entry points.
